import { DisplayContractorPipe } from './display-contractor.pipe';

describe('DisplayContractorPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayContractorPipe();
    expect(pipe).toBeTruthy();
  });
});
